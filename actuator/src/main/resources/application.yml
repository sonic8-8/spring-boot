management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    git:
      mode: "full"

  endpoints:
    web:
      exposure:
        include: "*"

  endpoint:
    shutdown:
      enabled: true
    health:
#      show-details: always
      show-components: always

info:
  app:
    name: "hello-actuator"
    company: "yh"