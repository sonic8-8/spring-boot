management:
#  server:
#    port: 9292

  info:
    java:
      enabled: true
    os:
      enabled: true
    git:
      mode: "full"

  endpoints:
    web:
      exposure:
        include: "*"
#      base-path: "/manage"

  endpoint:
    shutdown:
      enabled: true
    health:
#      show-details: always
      show-components: always

info:
  app:
    name: "hello-actuator"
    company: "yh"

logging:
  level:
    hello.controller: debug